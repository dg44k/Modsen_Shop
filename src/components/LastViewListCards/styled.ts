import { styled } from "styled-components";

export const WrapperViewLastListCards = styled.div`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  justify-items: center;

  margin: 40px 0 200px 0;
`;
